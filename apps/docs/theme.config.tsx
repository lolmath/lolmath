import { DocsThemeConfig } from "nextra-theme-docs";

const config: DocsThemeConfig = {
  logo: <span>lolmath</span>,
  project: {
    link: "https://gitlab.com/lol-math/lolmath",
    icon: <img src="/img/logo.svg" />,
  },
  // ... other theme options
};

export default config;
