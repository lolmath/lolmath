---
"@lolmath/ui": minor
---

Add `--lol-color-grey-400` (`#111318`), exposed to Tailwind as `lol-grey-400`.

It sits between `grey-300` and `hextech-black` — darker and quieter than
either — for a static elevated surface like a card or panel, so it reads as
raised content rather than the page floor or an interactive row.
