---
"@lolmath/ui": minor
---

Add `Group`, for visually merging several focusable controls into a single bordered field (e.g. a summoner name plus its `#`-prefixed tag), along with the borderless `GroupInput` and `GroupSeparator` segments meant to sit inside it.
