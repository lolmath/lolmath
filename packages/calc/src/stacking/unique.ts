import { notImplemented } from "../not-implemented";

/**
 * Computes the result of a set of effects of which only the strongest
 * applies, such as unique auras and slows.
 *
 * @param values The values of every effect.
 * @returns The value of the strongest effect.
 * @throws Not implemented.
 */
export function unique(values: readonly number[]): number {
	return notImplemented("unique", values);
}
